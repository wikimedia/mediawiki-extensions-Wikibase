These is the install file for the Wikibase Query extension.

Extension page on mediawiki.org: https://www.mediawiki.org/wiki/Extension:Wikibase Query
Latest version of the install file: https://gerrit.wikimedia.org/r/gitweb?p=mediawiki/extensions/Wikibase.git;a=blob;f=Query/INSTALL


== Requirements ==

Wikibase Query requires:

* MediaWiki
* Wikibase Repo extension
* Wikibase DataModel component
* Wikibase QueryEngine component

And the components those are dependent on.

And one of the following:

* MySQL 5 or later
* SQLite 3 or later

== Download ==

git clone https://gerrit.wikimedia.org/r/p/mediawiki/extensions/Wikibase.git Wikibase

== Installation ==

Once you have downloaded the code, place the ''Wikibase'' directory within your MediaWiki 'extensions' directory.
Then add the following code to your [[Manual:LocalSettings.php|LocalSettings.php]] file:

# Wikibase
require_once( "$IP/extensions/Wikibase/Query/WikibaseQuery.php" );

Run the maintenance/update.php script.
