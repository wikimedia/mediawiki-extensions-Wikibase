language: php

php:
  - 5.3
  - 5.4
  - 5.5

before_script:
  - composer install

notifications:
  irc:
    channels:
      - "chat.freenode.net#wikimedia-wikidata"
    on_success: change
    on_failure: always