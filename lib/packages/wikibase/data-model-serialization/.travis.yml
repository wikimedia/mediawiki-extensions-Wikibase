language: php

env:
  - THENEEDFORTHIS=FAIL

matrix:
  fast_finish: true
  include:
    - env: DM=~1.0@dev
      php: 5.3
    - env: DM=~1.0@dev
      php: 5.4
    - env: DM=~1.0@dev
      php: 5.5
    - env: DM=~1.0@dev
      php: 5.6
    - env: DM=~1.0@dev
      php: hhvm
    - env: DM=~1.0@dev
      php: hhvm-nightly
  exclude:
    - env: THENEEDFORTHIS=FAIL
  allow_failures:
    - php: hhvm-nightly

before_script:
  - composer require "wikibase/data-model=$DM" --prefer-source

script:
  - phpunit

notifications:
  irc:
    channels:
      - "chat.freenode.net#wikidata"
    on_success: change
    on_failure: always
    template:
      - "%{repository}/%{branch}/%{commit} : %{author} %{message} %{build_url}"
